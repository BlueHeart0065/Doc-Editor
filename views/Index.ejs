<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <input type="text" id="search-bar">
    <% if searchedDocs.length > 0 { %>
        <% for (let item of searchedDocs) {%>
            <div style="background-color: antiquewhite;">
                <p><%= item.title %> --- [ <%= item.updatedAt %> ]</p>
            </div>
        <% } %>
    <% } %>
    
    <form action="/docs/new" method="post">
        <button name="new" id="new">+ New Doc</button>
    </form>
    <% for (let doc of docs) {%>
        <li><p><%= doc.title%> - </p><a href="/docs/<%= doc.id %>/edit">Link</a></li>
    <% } %>
    <script src="/search.js"></script>
</body>
</html>